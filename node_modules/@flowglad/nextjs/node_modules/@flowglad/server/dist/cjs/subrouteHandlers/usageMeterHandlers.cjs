"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};
var usageMeterHandlers_exports = {};
__export(usageMeterHandlers_exports, {
  getUsageMeterBalances: () => getUsageMeterBalances
});
module.exports = __toCommonJS(usageMeterHandlers_exports);
var import_shared = require("@flowglad/shared");
var import_serverUtils = require('../serverUtils.cjs');
const getUsageMeterBalances = (params, flowgladServer) => __async(null, null, function* () {
  let error;
  let status;
  let data = {};
  if (params.method !== import_shared.HTTPMethod.POST) {
    error = {
      code: "Method not allowed",
      json: {}
    };
    status = 405;
    return {
      data,
      status,
      error
    };
  }
  try {
    const parsedParams = import_shared.getUsageMeterBalancesSchema.parse(
      params.data
    );
    const result = yield flowgladServer.getUsageMeterBalances(parsedParams);
    data = result;
    status = 200;
  } catch (e) {
    if (e instanceof Error) {
      error = (0, import_serverUtils.parseErrorStringToErrorObject)(e.message);
    } else {
      error = {
        code: "Unknown error",
        json: {}
      };
    }
    status = 500;
  }
  return {
    data,
    status,
    error
  };
});
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getUsageMeterBalances
});
//# sourceMappingURL=usageMeterHandlers.cjs.map