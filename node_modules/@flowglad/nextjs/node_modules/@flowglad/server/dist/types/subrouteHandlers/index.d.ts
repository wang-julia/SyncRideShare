import { type AuthenticatedActionKey, FlowgladActionKey, type HybridActionKey } from '@flowglad/shared';
import type { HybridSubRouteHandler, SubRouteHandler } from './types';
export declare const routeToHandlerMap: {
    [K in AuthenticatedActionKey]: SubRouteHandler<K>;
};
export declare const hybridRouteToHandlerMap: {
    [K in HybridActionKey]: HybridSubRouteHandler<K>;
};
/**
 * Runtime check for whether an action key is a hybrid route.
 * Used by requestHandler to determine auth behavior.
 */
export declare const isHybridActionKey: (key: FlowgladActionKey) => key is HybridActionKey;
