import "./chunk-CRTRMMJ7.js";
const isValidURL = (url) => {
  try {
    new URL(url);
    return true;
  } catch (e) {
    return false;
  }
};
const validateUrl = (url, propName, allowRelative = false) => {
  if (typeof url === "undefined") {
    return;
  }
  const isValid = allowRelative ? url.startsWith("/") || isValidURL(url) : isValidURL(url);
  if (!isValid) {
    const expectedMsg = allowRelative ? "a valid URL or relative path starting with a forward slash (/)" : "a valid URL";
    throw new Error(
      `FlowgladProvider: Received invalid \`${propName}\` property. Expected ${expectedMsg}. Received: "${url}"`
    );
  }
};
export {
  validateUrl
};
//# sourceMappingURL=utils.js.map